spring:
    application:
        name: to-do-service
        scheduled:
            task:
                update-status:
                    fixed-delay: 60000 # Runs once every minute
                    initial-delay: 60000 # Runs once every minute